[Memory Bank: Active]

This file is local-only and noncanonical. Durable project facts belong in [`docs/maintainer-wiki/index.md`](docs/maintainer-wiki/index.md).

Recent refresh: Carbon Memory was refreshed on 2026-06-24 without any newly discovered durable repository changes beyond the already recorded environment-bundle export-hardening work. The latest stable implementation context still centers on [`src/core/config/importExport.ts`](src/core/config/importExport.ts:1), where export hardening preserves importable rules, workflows, skills, mode-rules, and custom-mode-like content with conservative redaction while import hardening prevents MCP config secrets from being reintroduced through bundle import.

Current local security note: provider/global settings sanitization still uses [`isSecretStateKey()`](packages/types/src/global-settings.ts:336) to strip secret-state fields before bundle serialization. MCP config export remains structurally sanitized through [`sanitizeMcpConfigForBundleExport()`](src/core/config/importExport.ts:234), and imported MCP bundle content is now sanitized again inside [`importEnvironmentBundle()`](src/core/config/importExport.ts:609) before [`safeWriteJson()`](src/core/config/importExport.ts:641) persists it.

Current local export-scope note: public bundle export now retains importable rules, workflows, skills, mode-rules, and custom-mode-like file contents in sanitized form instead of omitting them. Provider/global settings still strip secret-state values through [`isSecretStateKey()`](packages/types/src/global-settings.ts:336), and MCP JSON secret-bearing fields remain redacted during export/import sanitization. Legacy settings export remains unchanged in [`exportSettings`](src/core/config/importExport.ts:731); hardening applies to environment bundle export through [`exportEnvironmentBundle()`](src/core/config/importExport.ts:765).

Test coverage in [`src/core/config/importExport.spec.ts`](src/core/config/importExport.spec.ts:1) now covers provider secret stripping, MCP secret removal with safe field preservation, and preservation of importable rule/workflow/skill-style content under conservative redaction.

Validation proof from [`src/package.json`](src/package.json:1): [`pnpm test core/config/importExport.spec.ts`](src/package.json:1) passed with 1 file / 14 tests from [`src`](src/package.json), and [`pnpm check-types`](src/package.json:1) passed from [`src`](src/package.json). No servers were started in this work loop.

Refresh process note: slash-command execution still rejected `/understand`, but the local Understand Anything plugin was available at [`C:/Users/VSCode/.understand-anything-plugin`](C:/Users/VSCode/.understand-anything-plugin). During this refresh, [`.understand-anything/.understandignore`](.understand-anything/.understandignore) was generated and the deterministic scanner at [`scan-project.mjs`](C:/Users/VSCode/.understand-anything-plugin/skills/understand/scan-project.mjs:1) was run successfully, updating [`.understand-anything/intermediate/scan-result.json`](.understand-anything/intermediate/scan-result.json) with `filesScanned=3397`, `filteredByIgnore=0`, and `complexity=very-large`. Full graph assembly still was not completed in this session.

Workflow/command diagnosis note: the failed tool invocation path was confirmed to be a command-vs-workflow mismatch rather than missing Understand Anything files. Tool-based slash execution resolves only command files through [`getCommands()`](src/services/command/commands.ts:127) and [`scanCommandDirectory()`](src/services/command/commands.ts:269), while chat `/slash` handling resolves enabled workflow files through [`parseKiloSlashCommands()`](src/core/slash-commands/kilo.ts:26), [`refreshWorkflowToggles()`](src/core/context/instructions/workflows.ts:30), and [`synchronizeRuleToggles()`](src/core/context/instructions/rule-helpers.ts:37).

Global workflow status note: understand-related global workflows were added under [`C:/Users/VSCode/.kilocode/workflows`](C:/Users/VSCode/.kilocode/workflows), including [`understand.md`](C:/Users/VSCode/.kilocode/workflows/understand.md), [`understand-chat.md`](C:/Users/VSCode/.kilocode/workflows/understand-chat.md), [`understand-dashboard.md`](C:/Users/VSCode/.kilocode/workflows/understand-dashboard.md), [`understand-diff.md`](C:/Users/VSCode/.kilocode/workflows/understand-diff.md), [`understand-domain.md`](C:/Users/VSCode/.kilocode/workflows/understand-domain.md), [`understand-explain.md`](C:/Users/VSCode/.kilocode/workflows/understand-explain.md), [`understand-knowledge.md`](C:/Users/VSCode/.kilocode/workflows/understand-knowledge.md), and [`understand-onboard.md`](C:/Users/VSCode/.kilocode/workflows/understand-onboard.md).

Verification note: rerunning the tool-backed slash path still exposed only built-in `init`, which is consistent with the current implementation because [`RunSlashCommandTool`](src/core/tools/RunSlashCommandTool.ts:14) does not consult workflow files.

Release-note status: [`CHANGELOG.md`](CHANGELOG.md) remains intentionally unchanged; user-facing release-note coverage should go through a changeset or normal release tooling if needed.

Implementation update (2026-06-24): extension manifest identity changed to `spex.spex-code` (`publisher=spex`, `name=spex-code` in [`src/package.json`](src/package.json:1)). Preferred project/global config directory is now [`.spexcode`](.spexcode), with [`.kilocode`](.kilocode) retained as a readable fallback for compatibility. Compatibility identifiers preserved: command IDs, settings namespace keys, storage keys, [`.kilocodemodes`](.kilocodemodes), and [`.kilocodeignore`](.kilocodeignore). Key changed areas include [`src/services/roo-config/index.ts`](src/services/roo-config/index.ts:1), [`src/core/config/importExport.ts`](src/core/config/importExport.ts:1), [`src/core/webview/webviewMessageHandler.ts`](src/core/webview/webviewMessageHandler.ts:1), [`src/services/skills/SkillsManager.ts`](src/services/skills/SkillsManager.ts:1), [`src/utils/kilo-config-file.ts`](src/utils/kilo-config-file.ts:1), [`src/utils/autoLaunchingTask.ts`](src/utils/autoLaunchingTask.ts:1), [`src/core/webview/kilorules.ts`](src/core/webview/kilorules.ts:1), [`src/services/mcp/McpHub.ts`](src/services/mcp/McpHub.ts:1), [`src/services/marketplace/SimpleInstaller.ts`](src/services/marketplace/SimpleInstaller.ts:1), [`src/services/marketplace/MarketplaceManager.ts`](src/services/marketplace/MarketplaceManager.ts:1), and [`src/shared/globalFileNames.ts`](src/shared/globalFileNames.ts:1).
