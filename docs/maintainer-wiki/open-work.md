# Open Work

- Verify whether a repo-specific documentation validation command such as `docs:check` should be added to [`package.json`](package.json).
- Add workflow notes for maintaining Carbon Memory alongside repository changes.
- Decide whether [`docs/file-locations.md`](docs/file-locations.md:33) should keep the CLI / agent-runtime section as historical product documentation or be split into a separate archival/reference page.
- Add a short maintainer note about the VSIX packaging gotcha from concurrent packaging/watch processes so future packaging failures are triaged faster.
